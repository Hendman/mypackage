from . import myModule
# """
# The dot (.) in relative imports refers to the current package.
# In this case, it imports the 'myModule' from the current package directory.
# This is equivalent to: from mypackage import myModule
# The relative import allows the package to import its own submodules
# without needing to know the absolute package name.
# """

